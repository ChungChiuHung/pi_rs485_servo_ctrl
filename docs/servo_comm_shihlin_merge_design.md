# servo_comm_shihlin 與 servo_comm_shihlin_50W 合併設計文件（草案）

狀態（**2026-09-19 更新**）：合併版 `servo_comm_shihlin_unified/` 已實作完成
並在真實硬體上驗證過主要功能（RTU 通訊、警報處理、JOG、絕對角度定位、
回原點、OSC/Art-Net），下方 §2～§6 是**早期設計討論的歷史記錄**，其中
「尚未動手」「暫緩」的字樣已不代表現況。之後新增的功能——PA28 增量/絕對
模式切換、PA23 EEPROM 保護、PA30 交握讀取絕對位置、電子齒輪比檢查、
SET HOME 開機提示——都**只有單元測試（319 項通過），尚未對真實驅動器驗證**。
**待實機確認的完整清單與驗證步驟見 §7；PR 模式 PATH 位置記錄的調查見 §8。**

> **2026-09-18 硬體確認（重大更新，推翻先前假設）：**
> 1. **實際能通訊的參數是 RTU、115200 baud、站號 1**——不是本專案目前
>    所有程式碼（`servo_comm_shihlin/`、`servo_comm_shihlin_50W/`、
>    `check_pa28.py` 的預設值）假設的 ASCII/9600 或 ASCII/115200。用
>    Shihlin 官方 `SHServo_Soft` 軟體透過驅動器 CN4 USB 埠讀取 PC22，
>    確認驅動器實際設定就是 RTU/115200。且確認 **USB(CN4) 與 RS-485(CN3)
>    不能同時使用**（手冊 §9.1 明載）——先前 RS-485 長期零回應，很可能
>    有一部分原因是測試時 USB 線仍插在 CN4 上，需要先拔除才能讓 RS-485
>    生效。
> 2. **PA28 = 0（不是 1）**——透過 RTU 讀取（位址 `0x0336`，word_length
>    1 與 2 兩種都測過，raw bytes 皆為 `00 00`/`00 00 00 00`，CRC 正確，
>    非解析錯誤）與官方 `SHServo_Soft` 軟體交叉確認一致，都是 0。手冊
>    p.17（§4127-4129）說明 PA28 的用途是「permit that the motor with
>    absolute encoder but is operated as an incremental motor」——也就是
>    說，這顆馬達雖然帶絕對值編碼器，但**目前被刻意設定成當增量式馬達
>    使用**，不是設定錯誤或未設定。
>
> **對 §2.4 方案 B 的影響：** PA32(APR)/PA33(APP) 手冊明載「valid only
> when PA28 is set as 1」，既然確認 PA28 目前是 0，方案 B **目前不能
> 直接套用**。需要使用者決定：(a) 維持增量模式、針對現有 0x0000/0x0024
> 32-bit 累加值另外設計 wraparound-safe 的處理方式（不倚賴 PA32/PA33），
> 或 (b) 把 PA28 改成 1、切換成絕對模式——但這是對驅動器的寫入操作，
> 且絕對值編碼器系統通常需要 CN5 後備電池才能在斷電後保留位置，需要先
> 確認電池是否已安裝，屬於 CLAUDE.md §4 硬體安全閘門範圍，需要明確
> 使用者確認才能執行，不是單純改一個設定值就結束。

> **2026-09-22 更新：已改為 (b)，PA28 = 1 對真機生效中。** 使用者確認
> SDH-BAT-SET 後備電池已安裝、馬達型號 SME-L04030MCB 為絕對型，依 §7.C
> C1 序列執行：`write_pa28.py --to 1`（讀回確認 1）→ 實體斷電上電
> （跳 AL.2A，符合預期）→ 再斷電上電（跳 AL.2C，符合預期）→
> `write_PA29_Initial_Abs_Pos()`（PA29=1）清除 AL.2C（PA31 讀回 0，
> 無故障位元；`read_absolute_position_pulses()` 讀到 -1，雜訊等級）→
> 斷電後 PD16/PD25 歸零導致 AL.12 重新出現（預期行為，非新問題）→
> 用既有的 `write_PD_16_Enable_DI_Control()` + `clear_alarm_12()` 清除
> （之後讀 0xFF 無警報，PA28 仍讀回 1，`read_absolute_position_pulses()`
> 讀到 -95，仍是雜訊等級）。**上面「PA28 = 0」與「方案 C（軟體端
> wraparound）」的結論自此成為歷史記錄，不再是目前的驅動器狀態**——見
> §7.C 對應項目的勾選與 CLAUDE.md §3 待同步更新。C2~C10（PA30 交握、
> APR/APP 版面、絕對位置與增量計數器同步、斷電重開後角度是否吻合等）
> 仍待驗證，見 §7.C。
>
> **其他待辦：** 既有的 `servo_comm_shihlin/`、`servo_comm_shihlin_50W/`
> 以及本專案 `check_pa28.py`/`diag_raw_serial.py` 的預設通訊參數（ASCII
> 模式）跟目前確認的真實驅動器設定（RTU）不符，這代表過去這兩份「既有、
> 號稱可運作」的程式碼，至少在目前這顆驅動器的 PC22 設定下，未必真的能
> 建立通訊——需要另外確認 PC22 是不是曾經被改過，或這兩份程式碼原本就是
> 對著不同設定的驅動器/情境驗證的。

## 1. 目標
把 `servo_comm_shihlin/` 與 `servo_comm_shihlin_50W/` 合併成一份程式碼，
透過一個 JSON 設定檔切換「要控制哪一顆馬達 / 哪種情境（scenario）」，
而不是像現在這樣維護兩份幾乎相同、只能「手動同步」的資料夾。

這其實就是 `main.py` 當初「暫停中」的計畫（見 CLAUDE.md §5）的一個縮小
版實作——只先處理 Shihlin 系列這兩個變種，還不牽涉 `servo_communication`
（Type 1 品牌）那邊。

## 2. 差異總表

### 2.1 純參數差異（可以直接放進 JSON config，沒有爭議）

| 項目 | servo_comm_shihlin | servo_comm_shihlin_50W | 建議 config key |
|---|---|---|---|
| Baud rate | 115200 | 9600 | `baud_rate` |
| 齒輪比換算（pulse/degree） | 349525.333... | 116508.444445 | ~~`base_pulse_per_degree`~~ → 改存 `gear_ratio`（見下方說明） |
| 原點絕對位置 `abs_home_pos` | 62369153 | 1184347 | `abs_home_pos` |
| P 暫存器 STA/STB/JOG（加減速時間、Jog 速度） | 無 | 有 | ~~`extra_p_registers`~~ → 兩邊都保留（見下方說明） |
| P 暫存器 PE03/PE04（PATH#1 definition/data） | 無 | 有 | ~~`extra_p_registers`~~ → 兩邊都保留（見下方說明） |

> **更新（已確認）：** 兩種馬達用的驅動器是同一款，所以 STA/STB/JOG、
> PE03/PE04 這幾個暫存器不是「50W 專屬」，而是驅動器本身就有的暫存器
> ——兩個 profile 都應該定義它們，不需要用 `extra_p_registers` 這種
> per-profile 開關來切換。原本 `servo_comm_shihlin`（原版）程式碼裡沒有
> 定義，單純是因為當初沒寫到，不代表原版馬達不支援。合併後這幾個暫存器
> 會直接寫進共用的 `servo_p_register.py`，兩個 profile 都能用，不放進
> JSON config。

這幾項單純是「同一份程式碼、代入不同數字」，適合做成
`motor_profiles.json` 裡的欄位，兩顆馬達各一組 profile。

> **`base_pulse_per_degree` 拆解確認：** 對照程式碼裡的註解、
> `docs/SDE_English_manual_UL_v107.pdf`（p.349「22-bit (4,194,304 pulses/rev)
> high resolution encoder」、p.7829「Calculation of electronic gear
> ratio」段落）與 git log，驗證你說的推導方式完全吻合——`base_pulse_per_degree`
> 是「編碼器解析度（encoder pulse/rev）× 減速比（gear ratio）÷ 360」算出來的：
>
> | | 編碼器解析度（manual 確認） | 減速比 | 輸出軸 pulse/rev | ÷360 = base_pulse_per_degree |
> |---|---|---|---|---|
> | 400W（原版） | 4,194,304（22-bit） | **30** | 125,829,120 | 349525.333... |
> | 50W | 4,194,304（22-bit） | **10** | 41,943,040 | 116508.444445 |
>
> （50W 的減速比 10:1 也對得上 git log 裡的 commit `d41dd5f change other
> gear ratio 10:1`，交叉驗證沒問題。）
>
> **修正說明：** 這份文件較早版本曾誤植編碼器解析度為 1,048,576（2^20），
> 雖然當時算出來的減速比（120/40）在數學上也剛好整除、看起來合理，但對照
> manual 明確寫的「22-bit (4,194,304 pulses/rev)」才發現算錯——兩個
> 2 的次方數字都能整除同一組 pulse/rev 總數是巧合，光靠程式碼裡的數字反推
> 無法唯一決定，必須對照原廠規格書才能確認。已更正為 4,194,304 / 30 / 10。
>
> 另外也對照了 `servo_p_register.py` 裡 PA06(CMX)、PA07(CDV) 電子齒輪比
> 暫存器的預設值皆為 1（且程式碼中沒有覆寫它們），確認電子齒輪比維持
> 1:1、沒有額外疊加——上面表格的減速比是純機械減速機的比例，不是電子
> 齒輪比設定值。
>
> 兩邊的編碼器解析度（4,194,304，22-bit）完全相同——這跟你之前確認「驅動器
> 都相同」是一致的，編碼器解析度是驅動器/馬達本體的固定規格，不是每個
> profile 各自不同的數字。真正因馬達/減速機而異的只有**減速比
> （gear_ratio：30 vs 10）**。
>
> 所以 JSON 不應該直接存目前這種手算好、看不出物理意義的浮點數
> （`349525.3333333333`），而應該存：
> * 一個**共用常數**：`encoder_pulses_per_rev: 4194304`（不放進 per-profile，
>   兩個 profile 共用同一個值）
> * 各 profile 各自的 `gear_ratio`（400W = 30，50W = 10）
>
> 程式啟動時用 `base_pulse_per_degree = encoder_pulses_per_rev * gear_ratio / 360`
> 動態算出來，不再把算好的浮點數字寫死在程式碼或 config 裡。這樣以後
> 減速比若有變動（或要支援第三顆馬達），只需要改 `gear_ratio` 這一個
> 有物理意義、可以直接對照驅動器/減速機規格表的數字，不用重新手算浮點數。

### 2.2 邏輯/功能差異（無法只靠參數切換，需要你逐項決定）

以下每一項都需要你確認「要保留哪一種行為」，或是「兩種都要保留、變成
可選功能」。**我不會自己選一邊當標準**——请在每一項後面標記你的決定。

| # | 差異項目 | `servo_comm_shihlin`（原版）行為 | `servo_comm_shihlin_50W` 行為 | 你的決定 |
|---|---|---|---|---|
| 1 | 取消連續讀取的安全機制 | **沒有** `cancel_continuous_reading()` 方法，也沒有對應的 `on_cancel` 事件監聽器 | 有完整的 `cancel_continuous_reading()`：停止讀取執行緒、重算角度、觸發 `on_cancel` 事件 | ✅ **採用 `_50W`** |
| 2 | OSC handler：連續運動控制 | 沒有 `set_countinuous_motion_handler` / `ctrl_continuous_motion_handler` / `cancel_loop_handler` 這三個 handler | 有這三個 handler，對應 TouchDesigner 的連續運動控制 | ✅ **採用 `_50W`** |
| 3 | `enable_speed_ctrl()` 參數簽名 | `enable_speed_ctrl(self, speed_rpm)` —— 只吃轉速 | `enable_speed_ctrl(self, speed_rpm=100, acc_time=5000, enable=True)` —— 多了加速時間與啟用開關，行為分支也不同 | ✅ **採用 `_50W`** |
| 4 | 角度追蹤演算法（`pos_step_motion_test` / 類似路徑） | 用 `self.target_angle = angle` 之後算 `diff_angle = self.target_angle - self.current_angle` | 用 `self.current_angle = angle` 之後算 `diff_angle = self.current_angle - self.previous_angle` | ⏸ **暫緩** — 需要先確認其他變數後再統整（見 §4） |
| 5 | `servo_on()` 重置行為 | 重置時保留 `previous_encoder`，並呼叫 `save_abs_home_pos(self.current_encoder)` 把目前位置存回設定檔 | 重置時單純把 `current_encoder`/`previous_encoder` 歸零，並清空 `accumulate_pulse`，**沒有**呼叫 `save_abs_home_pos` | ✅ **兩邊優點合併**（見下方 §2.3） |
| 6 | Logging 訊息與註解風格 | 訊息較長、有步驟編號註解（`# 1) ...` `# 2) ...`） | 訊息較精簡，用 `logging.basicConfig` 統一格式 | ✅ **兩邊優點合併，採標準 logging best practice**（見下方 §2.3） |
| 7 | 180 度以上目標角度的保護 | 沒有這個檢查 | `pos_step_motion_test` 開頭多了一段：若 `diff_pulses >= base_pulse_per_degree * 180` 就直接 return 0.0，不執行 | ❌ **不採用（2026-09-21 使用者決定）** — 這個限制是先前另一個應用的需求，本專案沒有；已移除。來源調查見 §7.G2 |

> 註：第 4、7 項牽涉到實際控制馬達移動角度的計算，屬於 CLAUDE.md §3
> 「硬體安全」範疇——這兩項合併前必須先確認清楚，避免馬達移動到錯誤角度。
> 兩項都還在暫緩狀態，尚未定案，實作階段不會動到這兩塊邏輯。

### 2.3 「兩邊優點合併」的具體做法（第 5、6 項）

**第 5 項 — `servo_on()` 重置行為：**
合併後建議的行為：
* 保留原版「讀取真實編碼器目前位置」的做法（`self.current_encoder = self.read_motor_feedback_pulses()`），而不是像 `_50W` 直接歸零——歸零假設馬達剛好在原點，讀真實值比較不會失真。
* 保留原版 `save_abs_home_pos(self.current_encoder)`，把目前位置持久化回設定檔。
* 同時採用 `_50W` 的 `self.accumulate_pulse = 0` 重置——這個欄位是 `_50W` 新增的狀態（累積脈衝數，用於連續運動追蹤），原版沒有這個欄位，但既然合併後的程式碼會包含 `_50W` 的連續運動功能（第 1、2 項已確認採用 `_50W`），這個狀態就必須在 `servo_on()` 時一併重置，避免殘留舊值。
* `previous_encoder` 的處理維持原版邏輯（保留舊值供比較），而非像 `_50W` 直接歸零。

**第 6 項 — Logging：**
合併後建議的做法：
* 採用 `_50W` 的 `logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')`，在模組層級統一設定一次，取代兩邊各自零散的呼叫方式。
* 使用 `logging.getLogger(__name__)` 取得 logger（而非直接呼叫模組層級的 `logging.info(...)`），符合 Python logging 的慣例作法。
* 訊息內容保留原版「有步驟脈絡」的可讀性（例如註明是讀取哪個階段、哪個暫存器），但精簡成單行、不需要額外的 `# 1) ...` 編號註解——註解本身可以拿掉，脈絡直接寫進 log 訊息裡即可。
* 一律使用 lazy `%s` 格式化（`logger.info("Current Encoder Value: %s", value)`）而非 f-string，這是 logging 模組的建議寫法（避免每次呼叫都先組字串，即使該筆 log 被 log level 濾掉也一樣）。

以上兩項的合併寫法會在實作（Green 階段）時真的套用；如果你看了觉得
方向不對，請在動手寫程式前先提出。

### 2.4 新發現：Encoder Overflow 風險（兩邊都有，尚未处理）

你提出「目前都沒考慮到 encoder 會 overflow 的問題」——查證後確認這是
**兩個資料夾都存在、合併前必須處理**的真實風險，不是單一變種的問題。

**驅動器規格（`SDE_English_manual_UL_v107.pdf` §8 Servo absolute
system，p.186-188）：**
* 這是**絕對值編碼器系統**（SME 系列，帶絕對型編碼器，需要後備電池），
  不是增量式編碼器。
* 官方定義的總脈衝數公式：`Total pulse counts = R × 4,194,304 + p`，
  其中 `R`（圈數）範圍 **-32768 ~ 32767**（16-bit 有號數），`p`（圈內
  脈衝）範圍 0 ~ 4,194,303（22-bit）。超出 ±32767 圈會觸發 `AL.29`
  警報。
* 驅動器本身有專門的狀態暫存器 `PA31 (APST)`，其中 bit0＝絕對位置遺失、
  bit1＝電池電壓過低、bit2＝**overflow**、bit4＝絕對座標系統尚未設定。
* 驅動器也提供 `PA32 (APR)` / `PA33 (APP)` 這組「圈數＋圈內脈衝」分開
  儲存的暫存器，範圍就是上面的 ±32767 圈全範圍。

**目前程式碼的實際讀法（兩個資料夾皆同）：**
* `read_motor_feedback_pulses()` 讀的是 `0x0000`（"Motor feedback
  pulses"），`read_motor_feedback_pulses_0x0024()` 讀的是 `0x0024`
  （英文版稱 "Translated motor feedback pulses"；中文 V1.07 標為電子齒輪比前，兩份手冊互相矛盾，見 §7.A2）——manual 明載這兩個都是
  **2-word（32-bit）** 暫存器，且 `ModbusResponse.get_value()`
  是用 `int.from_bytes(..., byteorder='big')`（**無號**、未處理
  wraparound）直接組出整數。
* 32-bit 只能表示到 2^32；而編碼器解析度是 2^22 pulses/rev，所以
  `0x0000` 這個原始計數器只要馬達軸轉滿 **2^32 / 2^22 = 1024 圈**就會
  無聲無息地歸零重來——完全不會觸發驅動器的 `AL.29` 或 `PA31` overflow
  旗標，因為那些是驅動器內部對「絕對座標系統」的監控，跟這個
  Modbus 讀出來的原始 32-bit 累加值是兩回事。
* 換算到輸出軸（減速後）：400W（減速比 30）約 **34.1 圈**、50W（減速比
  10）約 **102.4 圈**輸出軸旋轉後，`0x0000` 讀出來的數字就會 wrap
  一次，且完全沒有任何警示。
* 目前程式碼**完全沒有**讀取 `PA31` 狀態、沒有使用 `PA32`/`PA33`，也沀
  有任何 wraparound / modulo 修正邏輯——`diff_angle`、`pos_step_motion_by`
  等所有角度計算都假設 `current_encoder` 是單調、不會繞回的數字。

**額外的風險（manual p.187「Operation restriction」）：**
manual 明確列出「不適合搭配絕對值系統使用」的操作條件，其中包含：
**(1) Speed control mode and torque control mode**、**(3) Single way
rotation（單方向持續旋轉）**。而兩個資料夾都有的連續轉動功能
（`motionStart_CW`/`motionStart_CCW`、`speed_ctrl_action` 連續模式）
正好就是這種用法——也就是說，合併後想保留的「連續旋轉」功能，原廠
本來就不建議跟這顆絕對值編碼器搭配使用，這跟上面的 32-bit wraparound
問題是同一組風險的兩個面向（用越久/轉越多圈，位置資料越不可靠）。

**⚠️ 方案 B 已被推翻，見下方方案 C（2026-09-18 更新）**：方案 B 的前提是
PA28 會確認為 1（絕對模式）。實測結果 PA28 = 0（見文件開頭的硬體確認
區塊）——這顆驅動器目前是刻意設定成「帶絕對值編碼器、但當增量式馬達
使用」，PA32/PA33 手冊明載「valid only when PA28 is set as 1」，目前無效，
方案 B **不能直接套用**。以下方案 B 段落保留作為之後若改用絕對模式時的
參考，實際採用的是再下方的方案 C。

同時，上面提到「連續旋轉不適合搭配絕對值系統」這條操作限制，前提也是
「使用絕對值系統」——既然確認目前是增量模式（PA28=0），這條限制的
適用性也需要重新評估，不能直接照搬到目前的實際配置上。

<details>
<summary>方案 B（PA32/PA33，已推翻，僅供參考）</summary>

**方案 B 的實作範圍（待 §2.2 第 4、7 項一併確認後才動工，見下方）：**

* **新增暫存器定義** — `PA31`(APST)、`PA32`(APR)、`PA33`(APP) 目前
  `servo_p_register.py` 完全沒有定義（連 `PA28`(ABS)/`PA29`(CAP)/
  `PA30`(UAP) 也沒有），需要仿照現有 `calculate_address()` 機制新增。
  對照 manual 位址對照表，實際 Modbus 位址：
  * `PA31` (APST，狀態) → `0x033C`
  * `PA32` (APR，圈數) → `0x033E`
  * `PA33` (APP，圈內脈衝) → `0x0340`
* **新的讀取/角度計算邏輯** — 改成 `current_pulse_total = APR × 4,194,304 + APP`，
  取代現有 `read_motor_feedback_pulses()` 的單一 32-bit 讀值。
  * `APR`（圈數）必須用**有號**方式解析（manual 定義範圍含負值），
    現有 `ModbusResponse.get_value()` 的 `int.from_bytes(...)`
    **沒有** `signed=True`，這個 bug 本身也需要一併修掉（不能只加
    PA32/PA33 讀取、卻沿用同一個有問題的無號解析函式）。
  * `APP`（圈內脈衝）維持無號解析即可（範圍 0~4,194,303，本來就不會
    是負值）。
* **前置條件確認** — manual 註明 PA32/PA33 「valid only when PA28 is
  set as 1」（絕對模式）。目前程式碼沒有任何地方寫入或檢查 `PA28`，
  代表這個設定可能是驅動器出廠/安裝時就設好的硬體端設定，而非軟體
  控制——**這點需要你確認實際硬體上 `PA28` 是否已經是 1**，否則
  PA32/PA33 讀出來的值可能無效。
* **`PA31` 狀態旗標** — 雖然方案 B 主要是換讀取位址解決 wraparound，
  但既然要新增 `PA31` 定義，建議一併在每次移動前順便檢查 bit0/bit1/
  bit2/bit4，等於把方案 A 的異常偵測也免費包進來（新增的暫存器都
  在同一批修改內，不算額外的合併範圍）。

**這個決定會回頭影響 §2.2 第 4、7 項** ——兩項原本暫緩的角度追蹤演算法
與 180 度保護，現在有了明確、涵蓋全範圍的位置來源（PA32/PA33），可以
在這個基礎上重新設計，不需要再各自沿用原本兩邊都基於「單一 32-bit
累加值」的舊寫法。等你確認 `PA28` 硬體設定後，我會回頭跟你確認第 4、7
項要不要一併用新的位置讀取方式重寫。

</details>

**✅ 已決定並實作：方案 C（軟體端 unwrap，2026-09-18）** — 既然確認
PA28=0、方案 B 無效，改用純軟體方式解決 `0x0000`/`0x0024` 這組 32-bit
累加值的 wraparound 問題，不需要 PA28=1、不需要 PA32/PA33。

**原理**：利用「兩次輪詢之間馬達不可能移動超過 2^32 一半範圍」這個物理
事實（現有連續讀取迴圈是 100ms 一次，要在 100ms 內跑滿 2^31 個 pulse
等於每秒 214 億 pulse，任何伺服馬達都不可能達到）。因此每次讀到新的
原始值時，先算出「無號差量 mod 2^32」，如果這個差量超過一半範圍，就
代表其實是繞回去的，換算成對應的負值短距離差，再累加成一個不會被
32-bit 邊界打斷的「累積 pulse 數」。

**已實作**：`servo_comm_shihlin_unified/encoder_pulse_tracker.py`
（`EncoderPulseTracker` 類別，`reset()`/`update()`/`cumulative` 介面）+
`test_encoder_pulse_tracker.py`（11 個測試，涵蓋正常移動、正向/反向
繞回、邊界值、多次繞回、遮罩超出 32-bit 範圍的輸入等情境，皆已跑過
全過）。純讀取端計算工具，不送出任何指令、不碰序列埠，不受
CLAUDE.md §4 硬體安全閘門限制。

**限制**：只在程式連續執行期間有效，不記得程式重啟前發生過幾次繞回
——這跟現有 `abs_home_pos` 的既有假設一致（本來就是執行期間內比對，
不是跨重啟持久化的絕對位置系統），不是這個方案新增的限制。

**待辦（合併恢復時一併處理）**：`_read_continuously()` 迴圈（
`servo_control.py` 兩個版本裡都一樣的位置）需要改成呼叫
`EncoderPulseTracker.update(raw_encoder)` 取代直接使用
`read_motor_feedback_pulses()` 的回傳值去算 `diff_angle`；
`servo_on()`/`set_home_position()` 重置 `current_encoder`/
`abs_home_pos` 的地方，需要同時呼叫 `EncoderPulseTracker.reset()`
保持兩者同步。這部分屬於 `servo_control.py` 主控邏輯本體，仍在
「暫停中」的合併計畫範圍內，等該計畫恢復時一併整合。

## 3. 提議的 JSON 設定檔格式（草案，尚未實作）

```json
{
  "active_profile": "shihlin_400W",
  "encoder_pulses_per_rev": 4194304,
  "profiles": {
    "shihlin_400W": {
      "baud_rate": 115200,
      "gear_ratio": 30,
      "abs_home_pos": 62369153
    },
    "shihlin_50w": {
      "baud_rate": 9600,
      "gear_ratio": 10,
      "abs_home_pos": 1184347
    }
  }
}
```

`encoder_pulses_per_rev` 是驅動器/編碼器的固定規格，放在 profile 外層、
兩個 profile 共用。`base_pulse_per_degree` 不再直接存在 JSON 裡，而是
程式讀取設定後，用 `encoder_pulses_per_rev * gear_ratio / 360` 算出來。

程式啟動時讀 `active_profile` 決定要套用哪一組數值，之後如果要切換馬達，
只需要改這個 JSON，不需要改程式碼或維護兩份資料夾。

這只是初稿，欄位名稱、要不要拆成獨立檔案（例如 `motor_profiles.json`
獨立於 `servo_config.json`）都還可以再討論。

## 4. 尚未決定的事項

1. **第 4、7 項（角度追蹤演算法、180 度保護）** — 你標記為「需要確認其他
   變數後再統整」，暫緩定案。這兩項都牽涉 `base_pulse_per_degree` 相關的
   角度/脈衝換算，屬於 CLAUDE.md §3 硬體安全範疇，實作階段會先跳過、
   不合併這部分邏輯，等你確認完再回來處理。
   若「其他變數」是指齒輪比、編碼器解析度（pulse/rev）等硬體規格數字，
   `docs/` 資料夾裡的 SDE 驅動器說明書應該有相關規格表——需要的話我可以
   幫忙對照文件查證，你再確認是否吻合實際量測值。
2. ~~合併後程式碼放哪裡~~ **✅ 已決定：新資料夾 `servo_comm_shihlin_unified/`**
   （已建立。目前已從 `servo_comm_shihlin_50W/` 複製通訊底層並新增 PA28
   防呆檢查，見 §2.4/§4 第 4 項；`servo_control.py` 主控邏輯本體仍待第
   4、7 項定案後才搬入）。`servo_comm_shihlin/` 與 `servo_comm_shihlin_50W/`
   會保留到新版本通過兩個 motor profile 的真實硬體驗證後才考慮處理，
   不會現在就刪除或標記棄用。
3. **驗證方式** — 合併後怎麼確認兩個 profile 都還能正確控制對應的馬達？
   （例如：兩個 profile 各自的角度計算、原點回歸都要用真實硬體驗證過
   一輪，才算完成——依 CLAUDE.md §3，這需要你在場確認。）
4. ~~Encoder overflow 處理方式~~ **✅ 已決定：方案 B**（改讀
   `PA32`+`PA33`，見 §2.4）。剩下待確認：實際硬體上 `PA28` 是否已設為
   1（絕對模式），否則 PA32/PA33 讀出來的值無效。**已建立防呆檢查**
   `servo_comm_shihlin_unified/check_pa28.py`（純讀取、可直接在樹莓派
   上對真實硬體執行，測試見 `test_absolute_mode_check.py`，7 項全過）
   ——請在 Pi 上執行後回報結果。
5. **§2.2 第 4、7 項最終寫法** — 現在有了方案 B 的全範圍位置來源，這兩項
   可以基於新的 PA32/PA33 讀取方式重新設計，不用再沿用兩邊各自的舊
   「單一 32-bit 累加值」寫法。等 `PA28` 確認後會回來一起定案。

## 5. 目前進度小結

* ✅ 第 1、2、3 項：確定採用 `_50W` 的行為（連續讀取取消機制、OSC 連續
  運動 handler、`enable_speed_ctrl` 新簽名）。
* ✅ 第 5、6 項：確定採「兩邊優點合併」，做法已寫在 §2.3。
* ⏸ 第 4、7 項：暫緩，等你確認相關變數後再統整——這兩項也是唯一還會
  卡住「合併後程式碼放哪裡」與進入 TDD Red 階段的項目。
* ✅ **Encoder overflow（§2.4）** — 已決定採用方案 B（改讀
  `PA32`+`PA33`，涵蓋全範圍，順便修掉 `get_value()` 缺少 `signed=True`
  的 bug）。待確認硬體 `PA28` 是否已設為 1。
* ⏸ 第 4、7 項：現在可以基於方案 B 的新位置讀取方式重新設計，等
  `PA28` 確認後一起定案。

## 6. 下一步
等第 4、7 項確認後，我會把本文件更新成正式的實作規格（含最終的
`motor_profiles.json` schema 與程式改動範圍），才進入 TDD 的 Red 階段
（先寫測試）。第 1、2、3、5、6 項已經可以先寫進規格，但為了讓整份
`servo_control.py` 一次到位、避免分兩次改動同一批檔案，建議等 4、7 項
一起確認後再開始動工。（歷史記錄：合併已完成，現況與待辦見 §7。）

## 7. 待實機確認清單（2026-09-19 盤點）

本節列出**程式已寫好、但尚未（或因條件不足而無法）對真實驅動器驗證**的
項目。依 CLAUDE.md §4，會寫入驅動器參數、移動馬達或覆寫原點的步驟，
必須**使用者在場並明確確認**才能執行；標 🔒 者屬此類。單元測試（319 項）
只能證明程式邏輯，無法證明「驅動器實際行為符合手冊/我們的假設」——下面
每一項都是一個**尚未被證實的假設**。

### 7.0 目前狀態與阻塞條件

| 項目 | 狀態 |
|---|---|
| 已 commit（僅單元測試） | `ba0ee3d` PA28 絕對模式支援；`3482dd8` PA23 保護 + PA30 交握 |
| 尚未 commit（僅單元測試） | 電子齒輪比檢查與 GET STATE VALUE 新欄位、`read_motor_feedback_pulses` 等改名、SET HOME 開機彈窗與 `setHome` 動作 |
| **已在實機驗證過**（2026-09-18/19） | RTU/115200/站號 1；PA28=0；警報 12 清除；JOG 連續旋轉；定位模式與絕對角度移動；回原點；OSC 與 Art-Net 的絕對角度移動與真實回原點移動（見 CLAUDE.md §3）；GET STATE VALUE 前 9 個暫存器解碼；Art-Net Channel Monitor ch1–7；RS-485 收發顯示；Set Point 記錄/持久化；MOVE TO SET POINT 落點誤差 ≤0.001°（14.4311° vs 目標 14.4302°、0°→−0.0001°） |

**目前無法進行實機驗證的原因（需先排除）：**
1. 開發機偵測不到任何 COM 埠（USB-RS485 轉接器未接上）。
2. **編碼器後備電池（SDH-BAT-SET）未安裝**——§7.C 全部被擋。
3. 驅動器韌體版本未知（PA23=2 需 ≥106，見 §7.B）。
4. 需要有人在場做**實體斷電重開**（§7.B、§7.C、§7.D 都要）。

**建議執行順序：** A（唯讀）→ B（寫參數、不動馬達）→ D（SET HOME/彈窗）
→ E（補程式缺口後再驗）→ C（絕對模式，等電池到貨）。每階段通過再進下一階段。

### 7.A 唯讀確認（零風險，首次連上時一次做完）

- [x] **A1 電子齒輪比 PA06/PA07 是否 1:1。** **【2026-09-21 實機通過：PA06=1、PA07=1
  （測試 Pi 192.168.0.123，RTU/115200/站號 1，只讀）】** 步驟：啟動 app，看 log、
  「Electronic Gear」方塊或 GET STATE VALUE 的 CMX/CDV。通過：1/1（或
  兩者相等）。失敗：所有角度與定位換算的前提不成立（定位指令脈波會乘
  CMX/CDV，角度卻按原始編碼器脈波算）→ 先停用移動功能，從面板把
  PA06=PA07 改回相等再測。（PC32~PC34 的備用齒輪比由 DI 選擇，目前未檢查。）
- [x] **A2 `0x0000` 與 `0x0024` 的關係（手冊互相矛盾，已查明出處）。**
  **【2026-09-21 實機通過（CMX/CDV=1/1，只讀，JOG 10rpm）：靜止時兩者相等（差 0~1 pulse，
  轉動前 125833384/125833384、轉動後 138173671/138173673、回原點後 125833398/125833398）。
  轉動中（約 2.9 圈馬達軸、輸出軸約 35°）兩者差 76~92k pulse，正好等於「速度×兩次讀取間隔」
  （log 時間戳 0.112~0.129 s，反推 9.7~10.6 rpm，與實測 10.0 rpm 一致），沒有偏移或比例差。
  結論：1:1 下兩個位址是同一個計數；哪一份手冊對「前/後」的說法正確，在 1:1 下無法分辨，
  要等 CMX/CDV 非 1:1 才能判定（那種設定本專案不支援，見 A1）】**
  英文版：0x0000＝「Motor feedback pulses」、0x0024＝「Translated motor feedback
  pulses」（乘過電子齒輪＝齒輪比**後**）。中文 V1.07 第 305 頁（絕對位置讀取
  一節）：0x0000＝「馬達迴授脈波數(電子齒輪比**後**)」、0x0024＝「馬達迴授脈波數
  (電子齒輪比**前**)」，並寫明**一般建議使用電子齒輪比前的 0x0024**；第 256 頁的
  監控表則把 0x0000 寫成不分前後、0x0024 為「前」。兩份手冊對 0x0024 的說法
  相反。**影響：** 目前所有角度計算都建立在 0x0000；電子齒輪比為 1:1 時兩個位址
  應讀到相同的值，差異無關緊要（實機的絕對角度定位誤差 ≤0.001° 也與此相容）。
  一旦 CMX/CDV 不是 1:1，依中文版 0x0000 就是「後」的那一個，我們的換算會錯，
  所以 A1 是硬性前提。步驟：GET STATE VALUE 靜止時讀 `FBK_0000`/`FBK_0024`（會
  顯示比值），轉動軸後再讀。通過：1:1 下兩者相等（記錄結論到 §2.4）。
  **不要**因為中文版建議就把位置來源改成 0x0024——切換會改變計數起算/繞回，存檔
  的 `abs_home_pos` 也得重新換算，需等實機證明兩者的關係再決定（§7.F）。
- [x] **A3 PA 群組參數的資料長度與位址（手冊已明確，程式已對齊；僅剩實機核對值）。**
  手冊：PA 群組 50 個參數每個都是 32-bit（2 word），每個參數佔連續 2 個位址
  （PA23＝0x032C、PA31＝0x033C、PA32＝0x033E、PA33＝0x0340），讀取須以 2 word
  重組高低字。程式：`PA.calculate_address()` 算出的位址與手冊逐一相符，
  `_read_parameter()` 預設就是讀 2 word，故無需修改。實機只需在首次連線時核對
  PA23、PA31~PA33 的值與 `SHServo_Soft` 面板一致（低字/高字順序沿用已驗證過的
  0x0000/PD 群組做法）。
- [x] **A4 警報暫存器 0x0100~0x010A 為 Hex 編碼（手冊已明確，程式已對齊）。**
  中文 V1.07 第 260 頁：回傳 0x00FF＝無警報、0x0001＝AL.01、0x0012＝AL.12；且警報
  代號含 A~F（AL.0A、AL.2C…），BCD 無法表示。`NO_ALARM_CODES = {0, 0xFF}` 與
  `ABSOLUTE_SYSTEM_ALARM_NAMES`（以顯示碼的十六進位為鍵，如 AL.2A→0x2A）本來就
  依此建立，先前標註的「假設」已移除。首次真的出現 AL.2A/2C 時仍可順便對照面板，
  但不再是阻塞項。
- [ ] **A5 驅動器韌體版本。** 用面板診斷或 `SHServo_Soft` 讀取；決定 PA23=2 是否
  可用（≥106）。**（2026-09-21 查證：英文手冊只在面板診斷畫面列出「Software
  version(low/high)」，沒有對應的 Modbus 暫存器，只能人工在面板讀取。）**
- [x] **A6 通訊參數 PC20/PC21/PC22/PC23 與 `motor_profiles.json` 假設一致**
  **【2026-09-21 實機通過（只讀）：PC20=1、PC21=1（回覆延遲 >1ms）、PC22=0x56
  （y=5→115200、x=6→8N2 Modbus RTU）、PC23=0】**
  （RTU/115200/站號 1 已驗證；PC21 回覆延遲、PC23 逾時未讀）。GET STATE VALUE
  目前**沒有**列這四項（見 §7.E3）。

### 7.B PA23 EEPROM 保護（🔒 寫入驅動器參數；app **首次帶著驅動器啟動就會自動寫入**）

背景：PD16/PD25 是 EEPROM 參數，幾乎每個按鈕/OSC/Art-Net 動作都會寫，
手冊保證壽命約 10 萬次。中文 V1.07：PA23=0 可寫入；1＝不寫入 EEPROM 且斷電
後自動回 0；2＝不寫入且斷電後仍為 2（韌體 ≥106）。英文版手冊只列 0/1。

- [x] **B0 PA 群組參數的寫入功能碼（0x06 還是 0x10）。** **【2026-09-21 實機通過：首次寫
  PA23（0→2）用 0x06 直接被接受並讀回 2，log 沒有 `Drive rejected function 0x06` /
  `using 0x10`。0x10 的字序假設因此未被用到、仍未驗證（只在 fallback 路徑）】** 手冊：PA~PF 各組參數資料
  長度**全部是 2 word（32-bit）**；0x06 是「單字寫入」、0x10 是「多字寫入」；
  驅動器對不接受的請求回例外碼 01（指令碼錯誤）/02（位址錯誤）/03（範圍錯誤）。
  手冊沒有明文禁止對 2-word 參數用 0x06，但依規格 0x10 較「正統」，有人預期
  PA 群組用 0x06 可能被拒。**已有的實機證據：** PD16/PD25 同樣是 2-word 參數
  （手冊 §9.4 的 0x061E、0x0630 都標 2 word），程式每次 SERVO ON/OFF、清警報都用
  0x06 寫入且確實生效——所以「2-word 參數用 0x06 一律會失敗」與實機事實不符；
  PA 群組本身則**尚未實測**。我們的值（0/1/2）都放得進單一 word，0x06 會寫入
  完整的值。**程式的做法：** `_write_parameter()` 先用已驗證的 0x06；只有在驅動器
  **回例外**時才改用 0x10（[低字, 高字]，2 word）重試一次，成功後之後都直接用
  0x10；沒有回應（線路問題）或 CRC 錯誤則不重試；超過 16 bit 的值直接走 0x10。
  PA29（原本連例外回應都不檢查）也改走同一條路。重要的寫入（PA23、PA28）本來
  就會讀回驗證。**實機確認：** 首次寫 PA23 時看 log——沒有 `Drive rejected
  function 0x06` 警告即代表 0x06 可用；若出現警告並接著 `using 0x10`，代表需要
  0x10。**未驗證的假設：** 0x10 的字序 [低字, 高字]（與讀回的順序一致），只在
  fallback 路徑用到；若驅動器回範圍錯誤 03，就是字序相反。
- [x] **B1 首次啟動的自動寫入。** **【2026-09-21 實機通過：PA23 原值 0；app 首次連線 log
  `EEPROM write protection enabled (PA23=2)`，`/status` 的 `eeprom_protection=2`】** 預期 log：`EEPROM write protection enabled
  (PA23=2)`，且「EEPROM Protect」方塊顯示 On。先用面板/`SHServo_Soft` 記下 PA23
  原值。注意：PA23 從 0 改成 2 這一下本身會寫一次 EEPROM（一次性，可接受）。
- [ ] **B2 舊韌體 fallback（寫 2 被拒絕或被靜默忽略 → 讀回驗證 → 改寫 1）。**
  **【2026-09-21：測試驅動器接受並保留 PA23=2，故 fallback 分支未走到，仍只靠單元測試；
  這暗示韌體 ≥106，但 A5 仍待面板確認】**
  若實機韌體 ≥106 就走不到這條分支，只能靠單元測試；若 <106 則必須實測：
  確認 fallback 真的走到、最終為 1。
- [ ] **B3 斷電後行為。** 🔒 需實體斷電。PA23=2 應維持 2；PA23=1 應回到 0，
  且 app 在 ≤30 秒（背景 guard）或下一個動作前重新套用。驗證方塊由 OFF 轉回 On
  的時間。注意 guard 執行緒只在 `python app.py`（`__main__`）啟動，若之後改用
  gunicorn/systemd 呼叫 flask，需另外啟動它（§7.E7）。
- [ ] **B4 保護生效時所有功能仍正常** **【2026-09-21 部分通過（PA23=2 生效中）：SERVO ON/OFF、
  CLEAR ALARM 12（隨 SERVO ON）、JOG（CW/CCW，`speed_rpm=10`）皆即時生效；SERVO OFF 後
  警報碼為 0x12（程式設計如此）。JOG 設 `speed_rpm=10`：第一次量測用「每秒取樣」換算得
  11.7~13.3 rpm，**那是取樣間隔被低估造成的假象**（實際間隔含 HTTP/讀取時間，比 1 s 長）；
  之後用 app log 時間戳精確計算為 **9.95~10.03 rpm**，與設定值一致。CW 時編碼器計數遞減。
  之後同一天又在 PA23=2 下驗證：定位模式移動（HOME、MOVE TO SET POINT、OSC `/set_point`、
  Art-Net ch4）全部即時生效、落點正確（見 §7.E1）。尚未測：長時間運轉、Art-Net 連續模式（ch1~3）、
  OSC 其他位址】**（**關鍵假設**：PA23=1/2 下 PD16/PD25 的
  RAM 寫入仍即時生效）：SERVO ON/OFF、CLEAR ALARM 12、JOG、定位、HOME、
  OSC、Art-Net。任何一項失效即表示此假設不成立。
- [ ] **B5 `write_PA28` 暫時解除/恢復 PA23。** PA23=1/2 時 PA28 不會存入 EEPROM，
  斷電後設定會消失，所以程式在寫 PA28 前把 PA23 寫回 0、寫完再恢復。待確認：
  (a) PA23=1 時寫 PA23=0 是否被接受（手冊說「所有參數不寫入 EEPROM」，含 PA23
  自己）；(b) 寫完斷電後 PA28 確實保留。是 §7.C 的前置。

### 7.C PA28 絕對模式（🔒 全部被「電池未安裝」阻擋）

前置：SDH-BAT-SET 已裝；馬達型號 SME-L04030MCB 為絕對型（使用者已確認，
建議再用銘牌與手冊型號編碼交叉核對）；馬達處於安全狀態、使用者在場並能
實體斷電；先完成 §7.A、§7.B。切換前務必先確認手邊有 §7.C1 的預期警報清單。

- [x] **C1 完整切換序列與各階段警報。** **【2026-09-22 實機通過：寫 PA28=1
  （讀回確認）→ 斷電上電跳 AL.2A（符合預期）→ 再斷電上電跳 AL.2C（符合
  預期）→ 寫 PA29=1 清除 AL.2C（PA31 讀回 0，無故障位元）→ 斷電導致
  PD16/PD25 歸零、AL.12 重新出現（既有已知行為，非新問題）→ 用
  write_PD_16_Enable_DI_Control()+clear_alarm_12() 清除，最終 0xFF 無警報、
  PA28 仍為 1。全程沒有出現 AL.24。詳見本文件開頭 2026-09-22 更新區塊】**
  寫 PA28=1 → 斷電上電（預期 AL.2A）→ 再斷電
  上電（預期 AL.2C）→ 用 PA29=1 或原點復歸清除。逐步核對：警報代碼（見
  §7.A4）、UI 警報名稱、PA31 各 bit 的變化。若出現 AL.24，須區分是「編碼器型別
  錯誤」還是「位置遺失/電池不足」（手冊兩種意思都有）。
- [ ] **C2 PA30 交握。** 量測寫 PA30=1 後多久回 0（程式上限 50×20ms=1 秒）；
  不寫 PA30 直接連續讀 PA32/PA33 是否會更新（決定能否降低通訊量）；PA30=2 的
  實際影響（會把位置命令重設成目前位置，可能中斷進行中的目標，目前只用 1，
  不要在動作中使用 2）。
- [x] **C3 PA32/PA33 的版面（兩份手冊相反）。** **【2026-09-22 實機通過：命令
  +90°/20rpm 移動（從 89.9969°到 179.9966°），移動前後分別讀 PA32/PA33 原始值：
  PA32(APR) delta=2096990（吻合預期圈內餘數 2097152）、PA33(APP) delta=7
  （精準吻合預期圈數 7＝90°×30 齒輪比÷360°）。**確認 PA33(APP)=圈數、
  PA32(APR)=圈內脈波，與程式碼現有預設（`abs_rev_register="APP"`，中文版
  手冊版面）完全吻合，不需要改設定。** 第一次嘗試時忘記先清 AL.12（斷電後
  PD16 歸零，`_execute_positioning()`內建的 clear_alarm_12() 沒有先重寫 PD16
  所以沒真的壓下 EMG），馬達沒有實際移動（安全的空轉，非硬體異常）；清除
  AL.12 後重跑即成功，見 `c3_check_apr_app_layout.py`。刻意設成相反版面確認
  拒絕使用的部分未測（只讀、低風險，可日後再補）】**
  中文 V1.07：PA32＝單圈脈波
  （0~4194303）、PA33＝有號圈數（−32768~32767）；英文版相反。預設採中文版
  （`abs_rev_register` 預設 `"APP"` 為圈數，可在 `motor_profiles.json` 各
  profile 覆寫為 `"APR"`）。步驟：手動轉軸恰一圈，看哪個暫存器在變。並**刻意
  設成相反版面**確認 log 出現 `Implausible absolute position` 且拒絕使用
  （只讀，安全）。
- [x] **C4 絕對位置與增量計數器是否同步前進。** **【2026-09-22 實機通過：
  靜止時 `_absolute_offset=0`，命令 +90°/20rpm 移動（179.9968°→269.9973°，
  誤差 -0.0027°）後 `_absolute_offset=24`，只漂移 24 個 pulse（相對 31,457,280
  pulse 的移動量，屬雜訊等級）。確認 `0x0000` 增量計數器在絕對模式下仍照常
  連續計數，與絕對值同步前進，連續讀取迴圈的固定偏移量設計成立，不需要改成
  每次輪詢都讀 PA32/PA33】** 連續迴圈用「絕對讀值 − 增量累計
  值」的固定偏移量，在單次 1 word 輪詢下回報絕對刻度。此設計**假設**絕對模式下
  `0x0000` 仍照常連續計數。通過：靜止與移動 ≥1 圈後偏移量變動只有數個 pulse。
  失敗：連續迴圈的角度顯示不可信，須改為每次輪詢讀絕對值（通訊量約 ×4）。
- [x] **C5（核心價值，斷電重開部分）** **【2026-09-22 實機通過：SET HOME
  （current_angle=0, abs_home_pos_absolute=-127 存檔）→ 命令移動 90°/20rpm，
  落點 90.0008°（誤差 +0.0008°，PA28=1 下第一次真實移動）→ 使用者實體斷電
  再上電 → 全新 ServoController、只呼叫 refresh_encoder_mode()（PA28 讀回仍
  為 1）與 `_refresh_current_angle_from_hardware()`，**沒有**呼叫
  set_home_position() → 讀回 89.9969°，與斷電前僅差 0.0039°（絕對脈波
  31457434 → 31456066，差約 1368 pulse，雜訊/回彈等級）；PA31 讀回 0、
  無故障位元。斷電後如預期出現 AL.12（PD16/PD25 歸零的既有已知行為，非
  新問題，用既有流程即可清除）。**核心價值（斷電不用重按 SET HOME）確認
  成立**】** MOVE TO SET POINT / HOME / back home 各入口的落點與存檔正確性
  未逐一測過（原始清單項目）
  （誤差目標比照增量模式的 ≤0.001°）；`abs_home_pos_absolute` 存檔正確；
  🔒 ~~斷電重開後不必重新 SET HOME 且角度一致~~——這是絕對模式的核心價值，
  已於上方確認成立。
- [ ] **C6 斷電中搬動軸。** 🔒 斷電時轉動軸（手冊限制 <50rpm）後上電，位置是否
  被正確追上；電池低壓時 AL.2D 與 PA31 bit1 在 UI 的呈現。
- [ ] **C7 JOG 與絕對模式並存。** 手冊把「速度控制模式」列為不適合絕對系統的
  操作。JOG 後 PA31 是否仍無故障、絕對位置是否仍與增量累計一致；若不一致，
  定位前需重新確認或標為不可信。（本專案決定 JOG 維持增量路徑、只讓定位支援
  絕對，此項驗證這個取捨是否足夠。）
- [ ] **C8 電池未裝／PA31 有故障時的降級行為。** `refresh_encoder_mode()` 會把
  `absolute_mode` 設為 True，但位置讀取會因 PA31 故障被拒絕：確認 UI 顯示
  警告、SET HOME/MOVE TO SET POINT 被拒絕而不是亂動、JOG 仍可用。
- [ ] **C9 切回增量（PA28=0）流程**：寫 0 → 斷電 → 「I POWER-CYCLED」→ 行為回到
  原狀。
- [ ] **C10 `/encoder_mode/adopt` 流程**：驅動器斷電期間 app 持續執行時的通訊失敗
  處理；上電後 adopt 正確採用新模式；pending 狀態顯示。
- [ ] **C11（不實測）** ±32768 圈溢位與 AL.29——現實上不易重現，僅靠單元測試。

### 7.D SET HOME 開機提示與增量原點的有效性

- [ ] **D1 驅動器斷電後增量計數器是否歸零/漂移。**
  **【2026-09-21 補充旁證：測試 Pi 上存檔原點 125832501、當時讀到 125832853，差 352
  pulse ≈ 0.001° 輸出角；仍只證明「app 重啟不影響」，驅動器斷電的情況未測】** 目前彈窗的理由是「增量計數器
  在驅動器上電時重新起算，先前存的原點失效」，但這**是假設**。步驟：讀 `0x0000`
  並記下 → 🔒 驅動器斷電上電（軸不動）→ 再讀，與存檔的 `abs_home_pos`
  （目前 199255060）比對。已有的旁證：2026-09-19 多次重啟 **app**（驅動器沒斷電）
  後，重新讀到的原始值與存檔原點仍吻合（角度誤差 <0.001°），也就是「只重啟
  app 不會讓原點失效」。因此現行「每次 app 啟動都彈窗」對「僅重啟 app」是
  偏保守的提醒；若 D1 證實只有驅動器斷電才失效，可改成偵測計數器突變再提醒，
  或維持現狀（使用者已要求每次重開都提醒）。
- [x] **D2 彈窗與 SET HOME 按鈕在瀏覽器實測** **【2026-09-21 通過：在測試 Pi 上以
  「軟體模擬的 RTU 驅動器」（不碰真實序列埠與 GPIO）跑 app，伺服器端：全新啟動
  `home_reminder_needed=True`、移動中 SET HOME 回 409、靜止時 SET HOME 回 200 且提醒
  消失；使用者用瀏覽器確認彈窗/按鈕行為正常。注意：驅動器是模擬的，真實驅動器上的
  SET HOME 仍隨 §7.E1、§7.D1 的實機項目驗證】**（原先只有伺服器端測試與 JS 括號檢查，
  **沒有實際開過瀏覽器**）：啟動後第一次載入會跳出；「Remind me later」與 Esc
  可關閉；「SET HOME NOW」成功後關閉並不再出現；移動中按下顯示 409 訊息；
  重新整理又出現；側欄 SET HOME 按鈕同一個對話框；窄螢幕排版。
- [ ] **D3 重設原點對已記錄 Set Point 的影響。** Set Point 存的是相對原點的角度，
  重設原點後 MOVE TO SET POINT 的實際目標位置會跟著改變。對話框已說明，但是否
  該在 SET HOME 後清除或標示既有 Set Point，待使用者決定。
- [ ] **D4 OSC `/set_home` 與 Art-Net ch11 也會讓提醒消失**（`home_set_since_start`
  由 `set_home_position()` 設定）——驗證遠端 SET HOME 後 UI 方塊轉為 Set。

### 7.E 已知程式缺口（先修程式，再回頭實機驗證）

- [ ] **E1【已修程式，2026-09-21 實機四個入口通過，剩「讀不到位置時拒絕」未測】絕對角度移動在 app 重啟後第一次會落在錯的位置。**
  **實機結果（測試 Pi，增量模式，全部 10rpm、加減速 5000ms；每個入口都是重啟後的第一個動作，起點都在非零位置，
  log 的 `Previous Angle` 為真實讀值 −2.57/14.4264/14.4266/14.4266，而非過期的 0.0）：**
  網頁 HOME −2.57°→+0.0012°；網頁 MOVE TO SET POINT 1 14.43°→−0.0014°；OSC `/set_point 0 5000 10`
  14.43°→−0.0015°；Art-Net ch4（`max_speed_rpm=20`、ch7=128→10rpm）14.43°→−0.0015°。另外 0→14.4302°
  （Set Point 2）三次落在 14.4314~14.4315°。**落點誤差 0.0012~0.0015°，略大於下面寫的 ≤0.001° 目標**
  （先前紀錄 0.0009°）；約 426~630 pulse（馬達軸），是否可接受待使用者決定。
  **未測：** 拔掉/關閉驅動器通訊時確認被拒絕而不亂動（需人工斷線）。
  原因：`post_step_motion_by()` 以 `current_angle` 計算相對位移，而它在連續讀取
  迴圈跑過之前一直是初始值 0.0（2026-09-19 實機重現：目標 14.43° 落在 28.86°）。
  受影響入口：網頁 HOME、MOVE TO SET POINT、OSC `/set_point`、Art-Net ch4。
  **修法：** 重新讀取位置改放進 `post_step_motion_by()` 本身；讀不到（或絕對模式下
  不可信）就丟 `PositionUnavailableError`（`ValueError` 子類）並**不移動**——網頁
  HOME 回 502 與原因、MOVE TO SET POINT 回 400、OSC 只記 log 且不回報成功、
  Art-Net 先消耗觸發邊緣再移動（失敗不會以 30~44 fps 重試，也不會中斷同一幀的
  其他通道）。單元測試涵蓋：陳舊角度 0.0 + 真實位置 14.43° 的位移計算、已在目標
  不移動、讀不到不移動、各入口的失敗行為。
  **實機驗證：** 重啟 app 後**立刻**、在什麼都還沒讀過的狀態下，從四個入口各做一次
  絕對角度移動（先手動移到非零位置），落點誤差 ≤0.001°；並拔掉/關閉驅動器通訊
  一次，確認被拒絕而不是亂動。
- [ ] **E2** `write_PD_16_Enable_DI_Control()` 仍在每個 `/action` 無條件重寫；
  可加「本次連線已切換」快取。有 PA23 保護後不再緊急，但能減少通訊量。
- [ ] **E3** GET STATE VALUE 加入 PC20/PC21/PC22/PC23，方便對照 `motor_profiles.json`
  的假設（§7.A6）。
- [ ] **E4** 連續讀取迴圈每 100ms 輸出多行 INFO log，Pi 部署前需降到 DEBUG
  （CLAUDE.md §2 的 SD 卡壽命考量）。
- [ ] **E5** 絕對模式下、第一次位置讀取之前，連續迴圈會退回用增量原點算角度
  （兩個刻度混用）。E1 修好後，所有移動入口都會先讀位置、同步偏移量，之後才啟動
  連續讀取，所以移動期間不再受影響；剩下的只是啟動後、尚未移動前 UI 顯示的角度
  仍是初始值 0（純顯示，不影響動作）。
- [ ] **E6** Art-Net Channel Monitor 的 ch8–12 解碼只有單元測試，未用真實 UDP 封包
  對運行中的伺服器驗證（動作本身已測過，且該測試會觸發真實動作，需使用者在場）。
- [ ] **E7** EEPROM guard 背景執行緒只在 `python app.py` 直接執行（`__main__`）時
  啟動；改用其他方式啟動（gunicorn、`flask run`）時不會有，部署方式確定後需補。

### 7.F 需要使用者決定（非硬體）

1. `abs_rev_register` 預設值：目前採中文 V1.07 版面，實機 C3 之後確認。
2. 電池取得與安裝時程；安裝前 §7.C 全部擱置。
3. D3：SET HOME 後是否清除既有 Set Point。
4. D1 之後決定彈窗觸發條件（每次 app 啟動 vs 偵測到計數器突變）。
5. 位置來源要不要從 0x0000 改成中文版手冊建議的 0x0024（A2）：只有在實機證明
   1:1 下兩者相等、且決定重新換算存檔的 `abs_home_pos` 之後才考慮；在此之前維持
   0x0000，0x0024 只用於 GET STATE VALUE 對照。
6. 訊號中斷保護的預設秒數（目前 2 秒）與 TD 的送出方式（G1/G2）——若 TD 只在值改變時才送，
   預設值就不適用。
7. 網頁密碼目前是「選用」（未設定時只在啟動 log 警告）；部署到 Pi 前是否要改成必須設定，
   或預設只綁 127.0.0.1。
8. 是否要實作 PR 模式 PATH 位置記錄（§8.5）。

### 7.G Art-Net 網路與安全（2026-09-20 新增，僅單元測試與本機 UDP 測試）

背景：主控電腦與雷射 DMX 轉換盒走有線、Pi（伺服馬達）走 Wi-Fi，全部經同一台
Router。程式端已加入：預設 Universe 改為 1、來源 IP 白名單、綁定 IP 欄位、
訊號中斷保護（預設 2 秒，停止連續轉動且不自動重啟）、ch10~12 預設忽略、
Art-Net 序號檢查、移除 `SO_REUSEADDR`、網頁選用密碼、`debug` 預設關閉。
以下都需要在真實網路與 TouchDesigner 上確認：

- [ ] **G1 TD 的送出行為。** 靜止數值時 TD 的 Art-Net Out 是否持續送出（以及 30/60 fps
  下的實際頻率）？觀察 Channel Monitor 的「frames/s」與「last frame … s ago」。
  若 TD 只在數值改變時才送，訊號中斷保護會誤判並停掉穩定轉動——那就必須把
  `signal_timeout_s` 設 0，或讓 TD 週期性重送。
- [ ] **G2 Wi-Fi 下的最大封包間隔。** 實測 Pi 走 Wi-Fi 時 frame age 的最大值（含 Router
  忙碌、Pi 省電模式開/關），把 `signal_timeout_s` 設在其上並留餘裕；確認不會在
  正常運轉中誤觸發。Pi 建議 `iw dev wlan0 set power_save off`。
- [ ] **G3 中斷保護實機動作。** 🔒 連續轉動中拔掉 TD 的網路/關掉 TD，確認馬達在設定
  時間內停止、UI 顯示 SIGNAL LOST；訊號恢復但 channel 1 仍為高時**不會**自己重轉；
  channel 1 歸 0 後才能重新啟動。
- [ ] **G4 單播與綁定 IP。** TD 的馬達 Art-Net Out 設單播到 Pi IP、Pi 綁定該 IP 後仍收得到；
  改成廣播時確認（預期）收不到，並確認改回 `0.0.0.0` 可收廣播。
- [ ] **G5 Universe 分離是否解決雷射干擾。** 雷射用 Universe 0、馬達用 Universe 1、皆單播後，
  雷射閃爍是否消失。若消失，原因多半是先前兩者共用 Universe 0（DMX 轉換盒把馬達
  通道當雷射資料輸出），而不是 Router 效能；若仍閃爍，才需要往 Router／廣播方向查。
  這一項需要使用者在現場、以真實雷射設備驗證。
- [ ] **G6 來源白名單與統計。** 從另一台電腦送同 Universe 的封包，確認被丟棄且
  「unlisted-sender」計數增加；別的 Universe 的封包計入「other-universe」。
- [ ] **G7 網頁密碼。** **【2026-09-21 部分通過（測試 Pi、模擬驅動器）：無帳密／錯密碼／錯帳號／空密碼
  對 `/index`、`/status`、`POST /action`、`POST /server/start`、靜態檔一律 401（含
  `WWW-Authenticate: Basic realm="Servo Control"`），正確帳密 200，未授權的 POST 未進入處理函式；
  瀏覽器登入視窗與輪詢正常。尚未驗證：PM2/systemd 環境變數傳入、
  `SERVO_WEB_HOST=127.0.0.1` 擋 LAN】** 設 `SERVO_WEB_PASSWORD` 後用瀏覽器確認登入視窗、錯誤密碼被拒、
  頁面上所有輪詢（/status、/log…）在已登入狀態下正常；以 PM2/systemd 啟動時環境變數
  有正確傳入；`SERVO_WEB_HOST=127.0.0.1` 時 LAN 上其他電腦連不上。
- [ ] **G8 ch10~12 預設忽略。** 未勾選時送 ch10/11/12 確認不動作且 log 有警告、Monitor 標
  IGNORED；勾選後才會作用（🔒 back home 是真實移動、set home 會覆寫原點）。

#### 7.G2 通道 1～3 速度控制、通道 5-6 編碼、通道 17（2026-09-21 修改，僅單元測試）

使用者回報：通道 1～3 速度控制無法正常控制、通道 5-6 不直覺、通道 13-16 會影響 5-6。
用模擬控制器重現後找到的問題與修正（全部只有單元測試，**尚未對真實驅動器驗證**；
任何馬達轉動依使用者規則必須 ≤20 rpm，且要使用者在場）：

- 啟動時**不送方向命令**：通道 2 先於通道 1 設定，或通道 1 降到 0 再升起而通道 2 沒變，
  驅動器進入 JOG 但沒人叫它轉。已改為每次啟動都重送方向。
- 通道 1 轉速**只在啟動那一刻生效**，運轉中改值無效。已改為變化時寫 0x0903（100 ms 限流，
  最新值優先，接收迴圈在沒有封包時也會補送）。
- 直接 CW↔CCW 被驅動端拒絕時，Art-Net 端當成已套用、不重試也不提示。已改為記錄、
  顯示在 Channel Monitor 與統計，通道 2 經 0 後再切換。
- 通道 3 取消後（已離開 JOG 模式），通道 1～2 仍繼續寫入失效的模式。已改為閂鎖：通道 1 回到
  0 之前忽略通道 1～2（不送多餘的停止寫入）。
- 通道 5-6 改為 0.01°/格、32768＝0°（可負，約 ±327°），與 13-14 同一套編碼；拿掉
  `position_mode_max_angle`（HTTP 選項、網頁欄位、建構參數）。
- 通道 13-16 改由**獨立的通道 17** 觸發，通道 4 只看 5-7、通道 17 只看 13-16，互不影響；
  時間為 0 時拒絕（不再退回 5-7）；同一幀兩個觸發同時出現時只執行絕對移動。
- 另外修正一個測試檔的問題：`test_refused_move_is_not_retried_...` 縮排在 `frame12()` 的
  `return` 之後，是永遠不會執行的死碼，已移回 `TestPositionModeChannels`。

- [x] **G9 運轉中改通道 1 轉速。** **【2026-09-21 實機通過（測試 Pi，`max_speed_rpm`＝20）：通道 1 從 64→255→128，
  實測馬達 −5.14／−20.48／−9.87 rpm，即時跟隨；JOG 中寫 0x0903 驅動器會立即套用】**
- [x] **G10 啟動順序。** **【2026-09-21 實機通過：通道 2 先設、通道 1＝0 時不動；通道 1 升到 255 後 −20.5 rpm；
  降到 0 停止；同方向再升起 −20.7 rpm。每次只有一次啟動與一次方向命令】**
- [x] **G11 直接反轉。** **【2026-09-21 實機通過：CW −10.04 rpm 時直接切 CCW 被拒絕（日誌警告、統計 refused＝True），
  仍以 −10.04 rpm 續轉；通道 2＝0 停止，再切 CCW＋9.64 rpm，refused 清除】**
- [x] **G12 通道 3 取消。** **【2026-09-21 實機通過：取消後 0 rpm、0x0901＝0、cancel_latched＝True；閂鎖中改通道 1、2
  無動作；通道 1＝0 解除後重新啟動 CCW＋9.97 rpm】**
- [ ] **G13 新編碼與通道 17。** **【2026-09-21 部分通過：通道 5-6 絕對移動 ±8°（值 44616＝32768＋11848）落點誤差 −0.002°／
  −0.003°；通道 17 相對移動 ±5° 距離 ±5.001°，伺服器算出 6 rpm。尚缺：負角度值的落點（當時角度為正；180° 限制
  已於 2026-09-21 移除，現在可測）、通道 17 實際耗時（腳本含固定等待，未量準）】**
- [ ] **G15 驅動器偶爾不回應剛進入 JOG 後的 0x0904 方向寫入。** **【2026-09-21 實機測試中發現並已修正：這個例外原本會跳出
  處理函式，導致啟動狀態沒記下，下一幀又重新執行整個啟動流程。現在啟動狀態先記下，方向寫入失敗只在
  0.3 秒後重試；3 個單元測試。修正後 G10～G13 重跑均正常】**
- [ ] **G14（未做）硬體呼叫仍在接收執行緒內。** 啟動邊緣的 `enable_speed_ctrl()` 至少卡住接收
  執行緒約 0.5 秒（5 次固定等待加約 9 次序列交易），期間封包排隊。若實機出現延遲或看門狗
  誤動作，再改成背景工作佇列。

#### 7.G3 移除 180° 單次移動限制（2026-09-21 使用者決定）

- **來源調查：** 2025-01-22（`92f3e4d`，commit 訊息只有「update」）加在回原點用的 `pos_step_motion_by()`，
  只擋正方向（沒有 `abs()`）；2025-02-05（`b4649a8`）使用者已把它從 `servo_comm_shihlin`（400W）拿掉，
  50W 版保留。2026-09-18 unified 改成雙向、擴大到 `post_step_motion_by()`（OSC `/set_point`、Art-Net、網頁
  HOME、MOVE TO SET POINT）；2026-09-20 移植回 ASCII 舊版時又被我加回去。沒有留下書面理由。
- **決定：** 使用者確認這是先前那個應用的需求，本專案沒有這樣的限制。兩個資料夾都已移除，不提供可設定的上限。
- **保留的檢查（硬體限制，不是應用限制）：** 指令脈波暫存器 0x0905／0x0906 只能放 0～2³¹−1（手冊約第 10416 行），
  超過會被截成錯誤的、較短的移動 → 以 `MoveOutOfRangeError`（`ValueError`）拒絕，什麼都不送（約 6144° 輸出軸，
  30:1）。非有限數（NaN、無限大）的角度同樣拒絕，且在讀取位置之前就拒絕。網頁 HOME 對此回 400。
- **後果：** 軸離原點 220° 時，HOME／回原點現在會真的走完 220°，不再被無聲拒絕；Art-Net 通道 5-6 的整個
  ±327° 範圍都可用。單位或數字打錯（例如 OSC 送 3000 而不是 30）會照送出去執行——首次使用新的送信端要用低速。
- [ ] **G16 超過 180° 的實際移動。** 尚未在真實驅動器上測（僅單元測試）：例如通道 17 相對 +190°（≤20 rpm 約
  50 秒），確認脈波高低字組、方向、落點與自動停止（靜止偵測）在長距離下都正常。需要使用者在場。

## 8. PR 模式 PATH 位置記錄（2026-09-20 調查）

問題：能不能透過 RS-485 寫入驅動器 PR 模式的 PATH（位置記錄），再用 PF82 觸發？
PR 模式內建 PATH#1~#63，每條由 32-bit 的「定義（PDEF）」與「資料（PDAT）」兩個
參數組成，速度／加減速／延遲從 PF 群組的表格用編號 0~F 引用。

### 8.1 專案現況：底層有一部分，但沒有 PATH 功能

| 項目 | 現況 |
|---|---|
| 暫存器定義 | **只有** PE01~PE04（ODEF、ODAT、PDEF1、PDAT1）與 PF82（PRCM）（`servo_p_register.py`）。沒有 PATH#2~#63、PF33~PF80（速度／加減速／延遲表）、PF85（PMEM）。 |
| PDEF 位元欄位編碼／解碼（TYPE、OPT、ACC、DEC、SPD、DLY） | **沒有**。 |
| 寫 PDAT（32-bit 位置脈波，含負值） | 底層可以：`_write_parameter()` 遇到超過 16 bit 或負值會自動走 function 0x10（低字、高字）。但沒有 PATH 專用的 API，也沒有對 PE/PF 群組實機驗證過。 |
| 寫 PF33~PF80 表格 | **沒有**。 |
| PF85（PMEM）PATH#1/#2 斷電不保持 | **沒有**。 |
| EEPROM 保護 | 有全域的 PA23（方法 B，見 §7.B）；沒有 PF85（方法 A）。 |
| 觸發 PATH（PF82） | `write_PF82()` 有，但**先前有 bug**：忽略傳入的 PATH 編號，永遠寫入 1，也就是每次都執行 PATH#1；既有測試還把這個錯誤行為當成預期。**已於 2026-09-20 修正**（寫入指定值；只接受 0＝原點復歸、1~63＝PATH、1000＝停止，64~999 依手冊禁止而拒絕；經 `_write_parameter()` 寫入並回報是否被驅動器接受），測試已更新。它**沒有接到任何**網頁／OSC／Art-Net 動作；舊資料夾 `servo_comm_shihlin/`、`servo_comm_shihlin_50W/` 的 `osc.py` 有呼叫，且同樣有這個 bug（未修，屬於已被取代的舊版）。 |
| 讀取 PATH 執行狀態 | `Read_Motion_Completed_Signal()` 只讀 PF82 並判斷「是否非 0」，沒有解讀 1000+n／2000+n。2026-09-18 實機發現它對現行「脈波定位」流程沒有意義（PR 執行才有意義）。 |
| 驅動器的控制模式 | GET STATE VALUE（2026-09-19 實機）讀到 STY＝0x0010＝位置模式、位置命令來源為「inner register」，也就是驅動器**已設成 PR 的位置來源**；但 PR 路徑從未實際執行過。 |
| 現行位置控制 | 走另一條路線：Positioning test（`0x0901=4`＋`0x0905/0x0906/0x0907`），與 PR 是兩套機制；此路線已在實機驗證。 |

結論：**要用通訊設定並執行 PR PATH，目前還做不到**；只有底層寫入 32-bit 參數的能力和一個（現已修正的）觸發函式。

### 8.2 提供的說明與手冊的核對

| 說明 | 手冊 | 結論 |
|---|---|---|
| PE03＝0x0704、PE04＝0x0706，各 2 word | PE 群組起始 0x0700，PEn＝0x0700＋(n−1)×2；所有參數群組皆 2 word | ✓ |
| PDEF 欄位：DLY、SPD、DEC、ACC、OPT、TYPE | TYPE=2/3（第 178 頁）：bit3~0＝TYPE（2＝SINGLE 定位完停止、3＝AUTO 定位完自動執行下一路徑）、bit7~4＝OPT（bit7~6＝CMD：00 絕對／01 相對／10 增量，bit5＝OVLP，bit4＝INS）、bit11~8＝ACC、bit15~12＝DEC、bit19~16＝SPD、bit23~20＝DLY、bit31~24 未用 | ✓（並補上各欄位位置；OVLP=1 時 DLY 必須為 0） |
| PDAT 範圍 −2³¹+1 ~ 2³¹−1 pulse | 非 index 模式如此；index 模式為 0~4194304（分度用途） | ✓ |
| SPD→PF33~PF48（rpm）；加減速→PF49~PF64（ms）；延遲→PF65~PF80（ms） | 相同；**ACC 與 DEC 共用同一張表（PF49~PF64）**，預設值 200~6000ms；速度預設 50~3000rpm | ✓（補充；已由中文版手冊確認 ACC／DEC 共用 PF49~PF64） |
| 「64 組」 | 是 PATH#1~#63 共 **63 組**（PATH 0＝停止／原點復歸）；PATH#1~#48 在 PE03~PE98，PATH#49~#63 在 **PF01~PF30**（中文版詳表：PF01＝PDEF49 … PF29／PF30＝PATH#63 的定義／資料） | 需修正（中文版手冊已確認 63 組） |
| 必須用 0x10 寫入 | 0x06＝單字寫入、0x10＝多字寫入；每個參數 2 word。PDAT 這種超過 16 bit 的值**必然**要用 0x10（程式已如此）；PDEF 的高位欄位（SPD、DLY）非 0 時同理。其餘是否 0x06 可用見 §7.B0 | ✓（PDAT/PDEF 部分） |
| PF82：0＝原點復歸、1~63＝執行 PATH、1000＝停止 | 相同，64~999 禁止；讀回值：1000+n 執行中、2000+n 定位完成（手冊寫了兩個 1003，其中一個應為筆誤） | ✓（完成判斷看 2000+n） |
| PF85（PMEM）設 0x0011 → PATH#1、#2 斷電不保持 | bit x＝PATH#1、bit y＝PATH#2，0＝斷電保持、1＝不保持，範圍 0~0x0011，所以 0x0011 正確。**只涵蓋 PATH#1 與 #2** | ✓；英文版手冊在 PF85 只寫了位元定義，但**中文版手冊有**註記「此參數主要提供使用者可以透過通訊不停的寫入新的目標點」（已由中文版確認）。PF85 只涵蓋 PATH#1、#2（X、Y 位數，其餘保留），其他 PATH 的頻繁寫入仍只能靠 PA23 |
| PA23=2 是全域保護 | 見 §7.B；PATH#3 以後頻繁寫入只能靠 PA23（韌體 ≥106 用 2，否則 1） | ✓ |

其他手冊細節（實作時要注意）：PF86/PF87（軟體行程限制）超出會產生 AL.14；行程限制與
PDAT 都是「電子齒輪比前的原始命令脈波」，因此仍以電子齒輪比 1:1 為前提（§7.A1）；
簡表中 PF01/PF02 的「definition／data」說明互相對調，與詳表矛盾（以詳表為準：中文版詳表
PF01＝PDEF49，需實機確認）。

### 8.3 若要實作的設計（尚未實作）

* **純函式模組**（不碰序列埠，可完整單元測試）：`encode_pdef(type, cmd, ovlp, ins, acc, spd, dec, dly)`、
  `decode_pdef()`、`path_registers(n)`（PATH#1~48 → PE(2n+1)/PE(2n+2)；PATH#49~63 → PF(2(n−48)−1)/PF(2(n−48))），
  並驗證範圍（PATH 1~63、編號 0~15、PDAT ±(2³¹−1)）。
* **ServoController：** `write_path(n, target_pulses, …)`（先寫 PDAT 再寫 PDEF，逐項讀回驗證，
  避免半成品被觸發）、`write_pr_tables()`（PF33~PF80）、`write_PF85_pmem()`、
  `execute_path(n)`（`write_PF82`＋輪詢到 2000+n，含逾時）、停止＝寫 1000。
* **不改動現有 Positioning-test 路線**；PR 走獨立入口（網頁區塊，OSC／Art-Net 通道另議）。
* **與 Set Point 的關係：** 目前 SET POINT 記錄的是相對原點的「角度」；對應 PR 要換成脈波
  （角度×`base_pulse_per_degree`），且要先釐清 PR 座標的零點（見 H4）。
* **安全（CLAUDE.md §4）：** 觸發 PR 移動需要使用者明確確認；網頁動作要有確認步驟、限速、
  移動前先確認 Servo ON 且不在 test mode。

### 8.4 需實機確認（PR）

- [ ] **H1 PE/PF 群組的寫入功能碼。** 同 §7.B0，另外 PDEF/PDAT 需 0x10（超過 16 bit 時程式自動使用）；
  確認 0x10 的字序 [低字, 高字] 正確（讀回值一致）。
- [ ] **H2 PR 是否可直接執行。** STY 已是 0x0010（inner register）；確認 PA01 是否還需其他設定、
  是否需要 Servo ON、必須不在 test mode（`0x0901` 要為 0）、與 DI（CTRG／PATH 選擇）的關係。
- [ ] **H3 PF82 觸發與完成判斷。** 寫 PATH 編號後讀回的 1000+n／2000+n 時序；卡住與逾時的處理；
  寫 1000 是否能中途停止。🔒 會移動馬達。
- [ ] **H4 PR 座標零點與換算。** PDAT 絕對命令的零點是什麼（上電位置、原點復歸、PA29）；與我們
  `abs_home_pos`／角度換算如何對應（含減速比 30 與電子齒輪比 1:1）。這是把 SET POINT 接到 PR 的前提。
- [ ] **H5 PDEF 欄位實測。** 寫 PATH#1（TYPE=2、CMD=絕對、SPD/ACC/DEC/DLY 各編號）後讀回一致，
  實際速度與加減速符合 PF 表內的值。
- [ ] **H6 PF85 的實際效果。** 寫 0x0011 後斷電，PATH#1/#2 是否回到預設；有 PA23 保護時是否還需要 PF85。
- [ ] **H7 EEPROM 寫入。** 頻繁更新 PDAT 時，PA23 保護是否確實擋住（同 §7.B4，量測面板上的 AL.0F 之外的
  間接指標即可）。
- [ ] **H8 PF82 讀值語意**（兩個 1003 的筆誤）與完成碼 2000+n。
- [ ] **H9 軟體行程限制 PF86/PF87 預設值**，長距離移動是否會 AL.14。
- [ ] **H10 與 Positioning-test 並存。** 兩條路線切換時是否需先寫 `0x0901=0`、Servo 狀態如何。

### 8.5 待你決定

1. 是否要實作 PR PATH 位置記錄（現行 Positioning-test 路線已實機驗證，PR 是另一套且需要 H1~H10）。
2. 若要做，先做哪一段：建議先做「純函式模組＋單元測試」（不碰硬體），再做讀取／寫入 API，最後才是觸發與網頁。
3. SET POINT 是否要對應到 PR（H4 之後）。

## 9. 待辦事項（2026-09-21）

### 9.0 阻塞條件：RS-485 目前完全沒有回應

**目前有兩條路徑，結果不同：**

* **測試 Pi 192.168.0.123（USB-RS485）：通。** 2026-09-21 唯讀讀到 PC20＝1、PC21＝1、
  **PC22＝0x0056（Modbus RTU、8N2、115200）**、PC23＝0（使用者當天口述的 0x53、
  「ASCII」不正確）。unified（RTU）可直接使用；`servo_comm_shihlin`（ASCII）**無法**與
  目前的驅動器通訊，除非把 PC22 改成 ASCII 值並斷電重啟。
* **Pi 3B+ 192.168.68.67（Waveshare RS485 CAN HAT，`/dev/ttyAMA0`）：完全沒有回應。**
  唯讀探測 54 種 PC22 設定組合（**已含正確的 0x56**）、兩塊 HAT、A/B 對調、拔掉 USB、
  驅動器斷電重開後，**沒有收到任何位元組**。已排除：序列設定不符、`config.txt`
  （已還原 `dtoverlay=disable-bt`）、USB 與 RS-485 同時使用。同一台驅動器在 USB 路徑
  上是通的，所以問題在 HAT 這條路（HAT、接線、CN3 端子），不在驅動器設定。

- [x] **T0 RS-485 連線恢復。** **【2026-09-21 已在測試 Pi 192.168.0.123 恢復：USB-RS485
  （FTDI FT232R，`/dev/ttyUSB0`）＋unified 的 RTU 程式碼，115200/站號 1 全部讀取成功；驅動器 PC22
  為 0x56（RTU）。Waveshare HAT 那條路徑仍不通，見上】** HAT 路徑還需要：(a) HAT 端與驅動器 CN3 第 5(A)/4(B) 腳的 A–B
  直流電壓、各線導通；(b) 用舊的 USB-RS485 轉接器在電腦上做相同探測，分辨是
  驅動器還是 HAT。（Pi 的 5V 電源開機時仍偶有 under-voltage，建議換更穩的電源。）

### 9.1 新項目：讀取 DO1～DO6 腳位即時狀態

手冊核對（`docs/en_manual.txt`）：使用者提供的三段內容**全部與手冊相符**。

| 項目 | 手冊 |
|---|---|
| 0x0205（1 word，唯讀）＝DO 腳位 ON/OFF | bit0～5＝DO1～DO6＝CN1_41～CN1_46（約第 10105～10120 行） |
| 0x020C／0x020D＝DO1～3／DO4～6 的功能碼 | 每個 DO 5 bit（範圍 0x00～0x1F）：bit4～0、9～5、14～10（約第 10177～10192 行） |
| DO 強制輸出 | 前置：無異警、Servo OFF → 0x0901 寫 0x0002 → 0x0203 寫 bit0～5 → 0x0901 寫 0x0000 離開（約第 10310～10343 行） |

專案現況（調查結果）：

| 功能 | unified（RTU） | servo_comm_shihlin（ASCII） |
|---|---|---|
| 讀 0x0205 | **內部有，沒有對外功能。** 只在 `read_mc_ok_status()` 裡用，且只為了在自動停止時交叉檢查 INP／CMDOK 兩個 DO，結果只寫 log。`ServoUtility.decode_do_status()` 有 DO1～DO6 的 bit 對應，但描述是佔位文字。沒有「讀 DO1～DO6」的獨立函式，網頁、OSC、Art-Net 都沒有 | 只有暫存器常數與 `decode_do_status()`，**沒有任何讀取** |
| 讀 0x020C／0x020D（功能碼） | **有**，同樣在 `read_mc_ok_status()` 內，5 bit 拆法與手冊一致，但只用來找 INP／CMDOK 在哪個 DO；沒有列出每個 DO 的功能名稱 | 沒有 |
| DO 強制輸出（0x0901＝2、0x0203） | **沒有。** 只有 `DO_CONTROL`（0x0203）常數；`read_test_mode_0x0901()` 讀 0x0901 且註解知道 2＝DO 強制輸出；`Enable_Position_Mode`／`Enable_JOG_Mode` 寫的是 0x0004／0x0003／0x0000，從未寫過 0x0002 | 沒有 |

待辦：

- [x] **T1 純函式與讀取（唯讀，不碰硬體前可先做）。**
  **【2026-09-21 完成（unified）：`ServoController.read_do_status()` 讀 0x020C、0x020D、0x0205
  （各 1 word，三次交易），回傳 DO1～DO6 的 {pin、on、function_code、function}；任何一次讀取
  失敗／解析失敗／無值都回 None（不會回「全部 OFF」）；`read_mc_ok_status()` 改為建立在它上面。
  新增 12 個單元測試（unified 共 429 項通過），測試資料取自 2026-09-21 實機樣本。
  實機驗證（測試 Pi，USB-RS485，唯讀，兩次讀取一致）：0x0205=0x26、0x020C=0x103、0x020D=0x825 →
  DO1 INP_SA off、DO2 ZSP ON、DO3 未指派 ON、DO4 TLC_VLC off、DO5 RD off、DO6 ALM ON。
  注意：DO6=ALM 在「無警報」時為 ON，DO3 未指派卻為 ON，都暗示 PD27（DO 接點定義）可能使
  「on」與「導通」極性相反——`read_do_status()` 回傳的是 0x0205 的原始位元，未套用 PD27（已寫進
  docstring），需在 T4 與 CN1 實際腳位／面板燈對照後才能下結論。`servo_comm_shihlin`（ASCII）
  尚未移植（T2）】**
  原需求：`read_do_status()`：一次讀
  0x0205 ＋ 0x020C／0x020D，回傳 DO1～DO6 各自的 {ON/OFF、功能碼、功能名稱
  （用現有 `BitMapOutput`）}；把 `read_mc_ok_status()` 改成呼叫它，避免重複；
  單元測試（含手冊的 bit 對應、5 bit 拆法、通訊失敗回 None 而非「全 OFF」）。
- [ ] **T2 對外顯示。**
  **【2026-09-21 unified 已完成、待 commit：新端點 `GET /io/do`（唯讀，`hardware_serialized`，
  讀取失敗回 503 而不是全 OFF，忙碌回 429，未連線回 503）＋網頁「Digital Outputs (DO1 - DO6)」
  面板（表格：DO、CN1 腳位、ON/off、功能名稱與代碼；『READ DO STATUS』按鈕；『Auto-refresh every
  2 s』核取方塊，預設關閉；註明 PD27 極性未套用）。新增 9 個測試（unified 共 438 項通過）。
  實機端到端（測試 Pi，USB-RS485，唯讀）：用 Flask 測試客戶端呼叫 `/io/do` 兩次都 HTTP 200 且與
  T1 樣本一致，整段執行建立 0 個寫入 frame。尚缺：在瀏覽器實際點按鈕與自動更新的目視確認；
  `servo_comm_shihlin`（ASCII）版尚未移植（該資料夾沒有序列鎖，只能按鈕讀取，且 reading_active
  時要拒絕；目前驅動器是 RTU，該版本也無法通訊）】**
  原需求：`/status` 或新的 `/io` 端點＋網頁「DO1～DO6」方塊（含 CN1
  腳位與功能名稱）。**預設不輪詢**（每次要多 3 次序列讀取；Pi 3B 的 CPU 與
  序列時序要留餘裕，CLAUDE.md §2），改成按鈕讀取或開啟後才輪詢。
  `servo_comm_shihlin`（ASCII）版另移植；該資料夾沒有序列鎖，不能背景輪詢。
- [ ] **T3 🔒 DO 強制輸出。** `force_do_outputs(mask)`：先讀 0x0100 與 0x0200 確認
  無異警且 Servo OFF，否則拒絕；寫 0x0901＝2 → 寫 0x0203 → 一定要在
  `finally` 寫 0x0901＝0 離開。0x0901 與 JOG／定位測試共用，需與它們互斥；
  預設不提供網頁／OSC／Art-Net 入口（會實際拉動外部設備的輸出）；需使用者
  在場確認（CLAUDE.md §4）。
- [ ] **T4 實機確認（依賴 T0）。** **【2026-09-21 部分完成（只讀）：0x0205=0x26（DO2、DO3、DO6 為 ON）；
  0x020C=0x103、0x020D=0x825 → DO1=INP_SA、DO2=ZSP、DO3=未指派(0)、DO4=TLC_VLC、DO5=RD、
  DO6=ALM；與「靜止、Servo OFF、無警報」一致。尚缺：(a) 與 CN1_41~46 實際腳位／面板 DO 燈對照
  （需人在現場）；DO3 未指派卻為 ON，可能是預設極性，未查證；(c) 強制輸出未測】** (a) 讀 0x0205 與 CN1 實際腳位（或面板 DO 燈）
  對照；(b) 讀 0x020C／0x020D，確認各 DO 的功能碼；注意：若 PA01 改成 Pr 模式
  （y＝1）且 u＝1，DI／DO 功能會隨模式改變，功能碼可能與 Pt 模式出廠值
  （2026-09-18 已確認）不同；(c) T3 需在 DO 未接負載或可安全動作時才測。

### 9.2 先前已討論、待使用者決定的項目

- [ ] **T5 PR 模式（方式 A）補齊。** 見 §8：PA01 寫入函式（`write_PA01_Ctrl_Mode`
  寫 0x0010，y＝1，但無人呼叫、無讀回確認）、PATH 參數（PDEF／PDAT）編碼與寫入
  完全沒有、PF82 只有寫入沒有完成判斷（1000+n／2000+n）且 unified 沒有觸發入口。
  待決定是否實作（§8.5）。
- [ ] **T6 PD16 寫 0x0FFF 的範圍。** 目前一次把 12 個 DI 全改成通訊控制（含 DI7＝EMG），
  比需要的大；是否縮小到只含 SON／EMG 等實際要用的位元，待決定（涉及安全，
  需實機確認實體急停行為）。
- [ ] **T7 把「定位寫入檢查與觸發重送」移植到 `servo_comm_shihlin`（ASCII）。**
  unified 已於 `ad52f56` 完成：`_execute_positioning()` 的五個設定暫存器（0x0901／0x0902／
  0x0903／0x0905／0x0906）改為「確認寫入並讀回」，失敗丟出 `DriveCommunicationError` 且不啟動；
  0x0907 觸發無回應時觀察編碼器 0.4 秒，沒在動才重送一次；網頁層回 502。實機依據：COM4
  20 次移動有 1 次觸發丟失且馬達沒動（另 Pi 上 Art-Net 28 次丟 2 次）；改善後 60 次全部到位。
  ASCII 版目前沒有這些保護（`_execute_positioning` 仍忽略寫入結果）。**依賴：** 需要一台
  實際講 Modbus ASCII 的驅動器才能實機驗證——2026-09-25 起 COM4 上的驅動器只回應 RTU
  （PA28＝0、增量型），2026-09-22 記錄的「ASCII、PA28＝1」已不符，若要驗證需先把 PC22 改回
  ASCII（🔒 寫入驅動器參數，需斷電重啟、使用者在場）。SET HOME 記憶體原點的同一個 bug
  已於 2026-09-25 移植完成（見 `servo_comm_shihlin/test_set_points.py`）。

### 9.3 2026-09-25 工作中提出、尚未決定的建議

- [ ] **Art-Net「忙碌保護」。** 冷卻（`32f51bd`，預設 0.5 秒）只擋連點；移動進行中再觸發
  （通道 4／17）不會被擋，驅動器會悄悄忽略第二個指令（2026-09-21 Pi 上互換測試已觀察到）。
  建議移動中的新觸發明確拒絕並寫日誌。
- [ ] **網頁 UI 按鈕鎖死。** `templates/index.html` 的 `sendCommand()` 送出前停用所有按鈕、
  等 `.always()` 才恢復，但 `$.ajax` 沒有逾時；網路掉包或 Pi 重啟時請求不會結束，按鈕永久鎖住
  （2026-09-21 觀察）。建議加逾時（例如 15 秒）並顯示「沒有回應」。
- [ ] **測試 Pi（192.168.0.123）穩定性。** 2026-09-21 約 21:28 無故重啟且 app 不會自動啟動；
  PC↔Pi 有偶發掉包（ping 20% 掉包、請求卡 12～54 秒）；Pi 同時接 eth0 與 wlan0（預設路由走
  手機熱點）；journal 不是永久保存，重啟原因查不到。建議：systemd/PM2 自動啟動、開啟永久
  journal、換電源與線材對照、停用不用的 Wi-Fi。
- [ ] **文件補齊。** Art-Net 冷卻時間（`trigger_cooldown_s`）尚未寫進 `OSC_ARTNET_GUIDE.md`，
  也沒有 `app.py` 的啟動選項。
- [ ] **G16／長距離經 Art-Net 驗證。** >180° 的單次相對移動（通道 17）尚未經 Art-Net 實機
  驗證；`ServoController` 直接呼叫已在 COM4 驗證 0→90→180→0（誤差 ≤0.0014°、758 個角度取樣
  無倒退）。
